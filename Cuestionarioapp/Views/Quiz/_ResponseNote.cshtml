@model UserResponse

<div class="alert alert-info">
    <strong>Tu Respuesta:</strong>
    <p class="mb-1">@Model.Response</p>
    <small class="text-muted">Enviada el: @Model.CreatedAt</small>
</div>